#include <iostream>
#include <vector>

int main(void)
{
    /*
    Constants
    ----------
    int G = 10;
    int M = 1;
    h (timestep)
    N (number of particles)
    */

    /*
    Initial state vector (initial positions and velocities)
    ------------------------------------------------------
    std::vector<double> initialState(2 * N);
        -Each element must be a vector with the data of x, y and z
    */

    return 0;
}